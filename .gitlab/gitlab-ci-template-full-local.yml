## Práctica 4 - GitLab CI/CD (template full - local)
##
## Este fichero NO contiene la solución final.
## Es una guía con ejemplos comentados para completar el pipeline.
## En este template se asume runner local con registry/Artifactory locales.

stages:
  - ci
  - cd

variables:
  # TODO (E2): Variables globales (en GitLab CI/CD Variables)
  # IMAGE_REPO: "scalian_training-java-hello-world"
  # REGISTRY_HOST: "local-registry:5000"
  # REGISTRY_REPO: "scalian_training-java-hello-world"
  # ARTIFACTORY_URL: "http://artifactory:8081/artifactory"
  # ARTIFACTORY_REPO: "libs-release-local"
  # COMPOSE_FILE: "docker-compose.yml"
  # COMPOSE_SERVICE: "app"
  # ENABLE_REGISTRY_PUSH: "true"
  # ENABLE_ARTIFACTORY_UPLOAD: "true"
  # RUN_CD: "true"

ci:build:
  stage: ci
  image: docker:27
  services:
    - docker:27-dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  script:
    - echo "TODO: build CI image (devops/ci.Dockerfile)"
    - echo "TODO: make lint/test inside CI image"
    - echo "TODO: docker build -t $IMAGE_NAME --build-arg VERSION=... -f devops/Dockerfile ."
    - echo "TODO: optional upload jar to Artifactory"
    - echo "TODO: optional push to local registry"

cd:deploy:
  stage: cd
  image: docker:27
  services:
    - docker:27-dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  rules:
    - if: '$RUN_CD == "true"'
      variables:
        ENV_NAME: "DEV"
      when: on_success
    - if: '$CI_COMMIT_BRANCH == "master"'
      variables:
        ENV_NAME: "PRO"
      when: on_success
    - if: '$CI_COMMIT_BRANCH == "develop"'
      variables:
        ENV_NAME: "DEV"
      when: on_success
    - when: never
  environment:
    name: $ENV_NAME
  script:
    - echo "TODO: docker compose up -d + logs"
  after_script:
    - echo "TODO: cleanup"
